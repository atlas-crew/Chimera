title: Chimera
description: Intentionally vulnerable monorepo for WAF testing and security education
url: "https://chimera.atlascrew.dev"
baseurl: ""

image:
  path: /assets/chimera-banner.png
  width: 1280
  height: 320
  alt: "Chimera — Intentionally vulnerable monorepo for WAF testing and security education"

twitter:
  card: summary_large_image

plugins:
  - jekyll-optional-front-matter
  - jekyll-titles-from-headings
  - jekyll-relative-links
  - jekyll-seo-tag
  - jekyll-sitemap

relative_links:
  enabled: true
  collections: true

titles_from_headings:
  strip_title: true
  collections: true

defaults:
  - scope:
      path: ""
    values:
      layout: "page"
  - scope:
      path: "index.md"
    values:
      layout: "home"

exclude:
  - Gemfile
  - Gemfile.lock
  - .DS_Store

sass:
  sass_dir: _sass
  style: compressed

markdown: kramdown
kramdown:
  syntax_highlighter: rouge
  input: GFM
